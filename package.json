{
  "name": "best-true-value-hardware",
  "version": "1.0.0",
  "description": "BEST TRUE VALUE HARDWARE",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/Server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
